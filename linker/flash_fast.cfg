# 快速烧录配置 —— 跳过校验，速度提升约 50%
# 仅用于开发调试，正式发布请使用 CFG.cfg（带 verify）
#
# 使用方法：
#   openocd -f linker/flash_fast.cfg -c "program build/Debug/OLED_Test1.0.bin 0x00000000 reset exit"

source [find interface/cmsis-dap.cfg]
transport select swd
source [find target/ti/mspm0.cfg]
adapter speed 10000

# 初始化并烧录（无 verify）
init
targets
